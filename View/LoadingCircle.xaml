<UserControl x:Class="View.LoadingCircle"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:View"
             xmlns:MaterialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid>
            <MaterialDesign:PackIcon Kind="Download" VerticalAlignment="Center" Width="30" Height="30" HorizontalAlignment="Center" Foreground="{DynamicResource PrimaryHueMidBrush}" Margin="0 0 0 15"></MaterialDesign:PackIcon>
        </Grid>
        <StackPanel VerticalAlignment="Center">
            <ProgressBar Style="{StaticResource MaterialDesignCircularProgressBar}" Height="50" Width="50" Value="0" IsIndeterminate="True" Foreground="{DynamicResource PrimaryHueMidBrush}" />
            <TextBlock HorizontalAlignment="Center" FontWeight="Light" Text="Đang tải dữ liệu" ></TextBlock>
        </StackPanel>
    </Grid>
</UserControl>
